services:
  redis:
    image: redis

pipeline:

  test:
    image: python:2.7
    commands:
      - pip install -r requirements.txt
      - pip install pytest
      - pytest tests.py -s

  deploy:
    image: fbcbarbosa/heroku
    login: fbcbarbosa@gmail.com
    app: hello-estabilis
    when:
      branch: master
    secrets: [ plugin_token ]

